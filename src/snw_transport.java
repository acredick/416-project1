import java.io.File;

public class snw_transport {
     /* MEMBERS */

     /* METHODS */
     // put: client to server
     public File transfer(File file, client sender, server receiver) {
          return null;
     }

     // get: server to client
     public File transfer(File file, server sender, client receiver) {

          return null;
     }

     // get: cache to client
     public File transfer(File file, cache sender, client receiver) {

          return null;
     }
}
